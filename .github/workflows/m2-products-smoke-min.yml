name: M2 Products Smoke Minimal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Smoke local aposentado; evidência mantida no PRF do M2."
